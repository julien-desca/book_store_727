{% extends 'base.html.twig' %}

{% block title %} Nouvel(lle) auteur(e) {% endblock %}

{% block body %}
    <h1>Création d'un(e) nouvel(lle) auteur(e)</h1>
    {# {{ form(form) }} #}
    {{ form_start(form) }}
        <div class="row">
            <div class="col-6">
                {{ form_row(form.firstName) }}
            </div>
            <div class="col-6">
                {{ form_row(form.lastName)}}
            </div>
        </div>
        <div class="row">
            <div class="col-6">
                {{ form_row(form.birthDate)}}
            </div>
            <div class="col-6">
                {{form_row(form.deathDate)}}
            </div>
        </div>
        {{ form_row(form.biography) }}
    {{ form_end(form)}}
{% endblock %}